# DuelNight — 1차 오픈 준비 현황 (기획자용)

작성일: 2026-05-12
문서 목적: 1차 개발이 끝난 시점에서 **무엇을 사용자에게 제공할 수 있는지**, **어떤 흐름으로 검증할지**, **남은 의사결정/이슈는 무엇인지**를 기획 관점에서 정리.

---

## 1. 서비스 한 줄 요약

> **여러 TCG(원피스 / 포켓몬 / 디지몬) 플레이어를 위한 통합 허브.**
> 카드 검색 · 덱 빌딩 · 컬렉션 관리 · 전적/승률 분석 · 매장/오프라인 매칭 · 공지/이벤트를 한 곳에서.

- 미리보기 환경: https://id-preview--91f6cdde-f492-45b3-be3f-4b2dc70d4752.lovable.app
- 정식 배포: https://duelnight.app

---

## 2. 사용자 유형과 권한

| 사용자 유형 | 접근 가능한 영역 | 비고 |
|--------------|------------------|------|
| **비로그인** | 랜딩 / 로그인 화면 | 데이터 작성 불가 |
| **일반 사용자** | 카드 / 덱 / 컬렉션 / 전적 / 리더보드 / 커뮤니티 / 프로필 | 본인 데이터 CRUD |
| **관리자** | 위 전체 + **관리자 콘솔** + **더미 데이터 운영 도구** | 권한 부여/회수, 데이터 검수 |

> 1차에서는 사이드바 메뉴 자체가 권한에 따라 다르게 보이도록 분리되어 있어, 일반 사용자에게 운영 메뉴가 노출되지 않습니다.

---

## 3. 1차 제공 기능 맵

### 3-1. 메인 / 온보딩
- 대시보드(홈) — 핵심 지표·바로가기
- 회원가입 / 로그인(이메일·Google) / 비밀번호 재설정

### 3-2. 카드·덱 (수집/구성)
- **카드 DB**: 게임·속성·코스트 등 검색·필터, 즐겨찾기, 리뷰
- **덱 빌더**: 덱 생성·저장·수정
- **컬렉션**: 보유 카드 등록/관리
- **팩 시뮬레이터**: 팩 개봉 체험

### 3-3. 플레이·통계
- **전적 기록**: 게임/기간 필터, 종합 승률(전체·선공·후공), 연승 streak, 승률 추이 차트, 덱·매치업·이벤트·상대 메타별 통계, AI 코치 코멘트, CSV/JSON 가져오기·내보내기
- **리더보드**: 게임/기간/최소 판수 기반 순위
- **티어 메이킹**: 사용자 평가 기반 티어 정리

### 3-4. 커뮤니티
- **매장 찾기**, **오프라인 매칭(LFG)**, **공지사항**, **이벤트 캘린더**

### 3-5. 계정
- **프로필** (닉네임/표시명/아바타)

### 3-6. 관리자 전용
- 관리자 콘솔(권한 grant/revoke, 관리자 목록)
- 시드 데이터 재생성
- 카드 자동 생성
- 데이터 검수 화면(테이블별 카운트/상태 점검)

### 3-7. 데모
- 샘플 데이터 둘러보기(샌드박스)

---

## 4. 사용자 시나리오 (스토리)

1. **신규 가입 → 첫 전적 등록**
   회원가입 → 게임 선택 → 첫 매치 결과 입력 → 승률/연승 즉시 반영 확인.
2. **컬렉션·덱 구성**
   카드 DB 검색 → 즐겨찾기 → 보유 등록 → 덱 빌더로 덱 저장.
3. **데이터 분석**
   전적 페이지에서 기간/게임 필터 변경, 매치업·이벤트별 승률 비교, AI 코치 코멘트 확인.
4. **커뮤니티 활동**
   주변 매장 검색, LFG 글 등록, 공지/이벤트 캘린더 확인.
5. **운영(관리자)**
   사용자 권한 부여, 더미 데이터 재시딩, 데이터 검수.

---

## 5. 1차 테스트 체크리스트

> 테스트 계정
> - 관리자: `admin@lovable.test / Admin123!`
> - 일반: `user1@lovable.test / User1234!`, `user2@lovable.test / User1234!`

| # | 시나리오 | 기대 결과 |
|---|----------|-----------|
| 1 | admin / user1 각각 로그인 | 사이드바 메뉴가 권한별로 다르게 노출 |
| 2 | 비관리자가 `/admin/*` 직접 접근 | 권한 안내 화면 표시(접근 차단) |
| 3 | 전적 추가 후 통계/차트/연승 확인 | 즉시 반영 |
| 4 | 게임·기간 필터 전환 | 통계·차트 재계산 |
| 5 | CSV 내보내기/가져오기 | 라운드트립 일치 |
| 6 | 카드 검색·필터·즐겨찾기·컬렉션 등록 | 정상 저장 및 표시 |
| 7 | 덱 생성·저장 | 목록에 즉시 노출 |
| 8 | 리더보드 기간/최소판수 변경 | 순위 갱신 |
| 9 | 관리자: 권한 grant/revoke | 사용자 메뉴 즉시 반영 |
| 10 | 관리자: 시드 재생성 / 데이터 검수 | 카운트 변화 확인 |

---

## 6. 콘텐츠 / 데이터 준비 현황

- 카드: 12장 시드 (OP12 기준 4장은 실제 이미지, **8장은 placeholder**)
- 컬렉션 6, 덱 2, 공지 2, 티어 리스트 1, LFG 1 게시글
- 매장/이벤트/티어 페이지의 **실데이터 운영 시나리오는 기획 보강 필요**

---

## 7. 알려진 이슈 / 의사결정 필요 항목

- [ ] **카드 이미지 보강** — placeholder 8장 교체 (라이선스/이미지 출처 결정 필요)
- [ ] **모바일 사이드바 UX** — 추가 검증 및 개선 여부
- [ ] **AI 코치** — 호출 빈도/비용/캐싱 정책 합의
- [ ] **매장/이벤트/티어 운영 시나리오** — 실데이터 입력 주체·주기 확정
- [ ] **공지/이벤트 작성 권한** — 관리자 전용 vs 별도 운영자 역할 도입 여부

---

## 8. 운영/배포 한 줄 정리

- 백엔드: Lovable Cloud (사용자 인증 · 데이터베이스 · 권한 분리 · 파일 저장 · 서버 함수)
- 프론트: TanStack Start v1 (SSR) + React 19 + Tailwind v4
- 배포: Lovable **Publish** 버튼 1회 클릭으로 갱신

---

## 9. 다음 단계 제안 (2차 우선순위 후보)

1. 카드 이미지/메타데이터 정식 임포트 파이프라인
2. 매장·이벤트 운영자 역할 추가 및 입력 폼 정비
3. 전적 데이터 기반 추천 덱/카드 추천 기능
4. 푸시/이메일 알림 (LFG 매칭, 공지)
5. 모바일 PWA 최적화 및 오프라인 캐싱

---

*본 문서는 `docs/PROJECT_STATUS.md`에 보관되며 `docs/COLLABORATION_GUIDE.md`와 함께 1차 검증·운영 기준 문서로 사용됩니다.*

---

## 10. 변경 이력 (2026-05-15)

### 덱 빌더 상세 페이지 수정
- **증상**: `/decks/$id` 진입 시 본인 비공개 덱이 "비공개 덱입니다"로 표시되거나 상세가 열리지 않음. 관리자도 타인 비공개 덱 열람 불가.
- **원인 1 (SSR 세션 부재)**: 라우트 `loader`가 SSR에서 `supabase.auth.getUser()`를 호출 → 서버에는 세션 쿠키가 없어 `currentUserId=null` → 항상 비로그인 판정.
- **원인 2 (관리자 RLS 부재)**: `decks` SELECT 정책이 `is_public OR auth.uid()=user_id`만 허용 → 관리자도 타인 비공개 덱 조회 불가.
- **수정 사항**:
  - `src/routes/decks.$id.tsx`: `loader` 제거, 클라이언트 `useAuth` + `useQuery`로 권한 판정 및 데이터 페칭 전환. `errorComponent`/`notFoundComponent` 추가.
  - 마이그레이션: `decks` 테이블에 `Admins can view all decks` SELECT 정책 추가 (`has_role(auth.uid(),'admin')`).
- **카드 멀티 일러스트 작업과의 관계**: 무관. 카드 갤러리/업로더 멀티 이미지 작업은 그대로 유지됨.

### 추가 수정 (같은 날, 후속 보고 반영)
- **증상 1**: 덱 상세 페이지 클릭 시 무반응. **원인**: `decks.$id.tsx` 컴포넌트에서 조건부 `return` 이후 `useQuery`/`useMemo` 훅을 호출 → 덱 로드 직후 React Hooks 순서 위반으로 컴포넌트 크래시. **수정**: 모든 훅을 조건부 return 이전으로 이동, `enabled: !!deck` 가드 추가.
- **증상 2**: 덱 레시피 탭이 안 보임. **원인**: 위 크래시로 인해 상세 페이지 자체가 렌더되지 않아 탭도 노출되지 않음. **수정**: 증상 1 수정과 동일.
- **증상 3**: 덱 추가 다이얼로그의 리더 입력이 일반 텍스트박스라 검색/선택 UX 부재. **수정**: `src/components/decks/deck-dialog.tsx`에 `LeaderPicker` 컴포넌트 추가 — `cards` 테이블의 `status='approved'` & `type='leader'` 카드들을 가져와 검색 가능한 Popover 드롭다운(이미지·코드 포함)으로 선택.

### 추가 수정 (2026-05-15, 덱빌더 RecipeEditor)
- **증상**: 덱 상세 → 레시피 탭 빈 화면, 카드 이미지 미표시, "+/−" 버튼이 한 번에 안 눌리고 두세 번 눌러야 반응. 콘솔: `cardMap.get is not a function`.
- **원인**: `RecipeEditor`의 `cardMap`을 `Map` 객체로 보관 → SSR 직렬화/구조적 공유 과정에서 `Map` 프로토타입 손실 → `.get()` 호출 시 throw. 결과적으로 `card?.image_url`이 항상 undefined가 되어 이미지/추가 동작이 모두 무력화됨.
- **수정**: `cardMap`을 `Record<string, CardRow>`로 변경하고 `useMemo`로 `.get()` 호환 래퍼 제공.
- **태블릿 UX**: +/−/삭제 버튼을 `h-8 w-8` (32px) + `touch-manipulation`으로 확장하여 터치 정확도 개선.

### 자동 검증 스크립트 (덱 상세)
- 파일: `scripts/verify-deck-detail.ts`
- 실행: `bun run verify:deck-detail` 또는 `bun run verify:deck-detail <deckId>`
- 검증: `decks` SELECT → `deck_cards` SELECT → `cards.in(code)` 매칭 → `image_url` 결손율 → optcg `leader` 카드 조회
- 종료 코드 1 = 실패 (CI에서 그대로 사용 가능)
- 주의: anon 키로 동작하므로 비공개 덱은 RLS상 검증 불가. 비공개 덱 검증이 필요하면 deckId를 인자로 넘기되 공개로 잠시 전환하거나, 별도 service-role 변형이 필요.
